import React from "react";
import { Swiper, SwiperSlide } from "swiper/react";

const Slider = ({ cildren }) => {
  return (
    <Swiper>
      <SwiperSlide></SwiperSlide>
    </Swiper>
  );
};

export default Slider;
